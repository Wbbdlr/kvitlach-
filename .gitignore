node_modules
.env
.env.local
frontend/.env.local
**/dist/
**/build/
backend/tmp/
*.log
*.DS_Store
# Rendered by `npm --prefix e2e run screenshots` -- a local view for looking
# at the layout, regenerated on demand and never reviewed as a diff.
e2e/screenshots/
e2e/test-results/
e2e/playwright-report/
e2e/blob-report/
# Generated by `vitest run --coverage`. The thresholds in
# backend/vitest.config.ts are the durable artefact; the HTML report is just a
# local view of one run and churns on every invocation.
**/coverage/
# Vitest's own last-run cache (which tests failed last time, for --changed).
**/test-results/
# Python bytecode cache from tools/card-mark.py.
**/__pycache__/
# deploy/verify-setup-admin.sh's own throwaway tree (see the deploy skill) --
# scratch output from a dry run against a stub docker, never meant to be kept.
/envtest/
